#!/bin/sh

set -eu

export MANGOHUD="${MANGOHUD:-1}"
export PROTONPATH="${PROTONPATH:-GE-Proton}"
export PROTON_FSR4_RDNA3_UPGRADE="${PROTON_FSR4_RDNA3_UPGRADE:-1}"
export PROTON_USE_NTSYNC="${PROTON_USE_NTSYNC:-1}"
export PROTON_USE_WOW64="${PROTON_USE_WOW64:-1}"
export RADV_TEX_ANISO="${RADV_TEX_ANISO:-16}"
export WINE_FULLSCREEN_INTEGER_SCALING="${WINE_FULLSCREEN_INTEGER_SCALING:-1}"

exec gamemoderun umu-run "$@"
